{
  "version": 2,
  "builds": [
    {
      "src": "BAC/backend/wsgi.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "15mb",
        "runtime": "python3.12"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "BAC/backend/wsgi.py"
    }
  ],
  "buildCommand": "uv pip install -r BAC/requirements.txt --system && cd BAC && python manage.py migrate --noinput && python manage.py collectstatic --noinput"
}